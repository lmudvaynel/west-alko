<%= render 'refinery/admin/search_header', :url => refinery.factory_photos_admin_factory_photos_path %>
<div class='pagination_container'>
  <% if @factory_photos.any? %>
    <%= render 'factory_photos' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
