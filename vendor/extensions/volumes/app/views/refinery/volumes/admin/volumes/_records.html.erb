<%= render 'refinery/admin/search_header', :url => refinery.volumes_admin_volumes_path %>
<div class='pagination_container'>
  <% if @volumes.any? %>
    <%= render 'volumes' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
