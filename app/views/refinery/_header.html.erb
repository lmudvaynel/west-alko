<div class="head">
  <div class="row">
    <div class="logo" id="logo">
      <img src="<%= image_path('logo.png') %>" alt="Official - Premium Multi-purpose HTML5 Template">
    </div><!-- end logo -->
  </div><!-- row -->
</div><!-- head -->

<%
  if I18n.locale.to_s == "en"
    @link_to_product = "/products"
    own = "Own"
    foreign = "Foreign"
    flood = "Flood"
  else
    @link_to_product = "/ru/products"
    own = "Свое"
    foreign = "Чужое"
    flood = "Разлив"
  end
%>

<div class="headdown my_sticky">        
    <div class="row clearfix">
          <nav>
            <ul class="sf-menu">
              <% first = true %>
              <% Refinery::Page.where("show_in_menu != ?",false).each do |page| %>
                <% if first == true %>
                  <li class="selectedLava">
                  <% first = false %>
                <% else %>
                  <li>
                <% end %>
                  <a href="<%= page.url %>"><%= page.path %></a>
                <% if page.id == 4 %>
                  <ul>
                    <li>
                      <a href="<%= @link_to_product %>?make=own"><%= own %></a>
                    </li>
                    <li>
                      <a href="<%= @link_to_product %>?make=flood"><%= flood %></a>
                    </li>
                    <li>
                      <a href="<%= @link_to_product %>?make=foreign"><%= foreign %></a>
                    </li>
                  </ul>
                <% end %>
                </li>
              <% end %>
            </ul><!-- end menu -->
          </nav><!-- end nav -->
        </div><!-- row -->
</div>